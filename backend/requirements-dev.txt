# P2-05 (external review, 2026-09-21): test-only tooling, split out of
# requirements.txt so the deployed image (Dockerfile only ever installs
# requirements.txt) never carries pytest/httpx into production. Install
# both together for local dev/CI with:
#   pip install -r requirements-dev.txt
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.2
# FastAPI's TestClient (used throughout tests/) is built on httpx -- never
# imported by app/ itself, only by the test suite.
httpx==0.28.1
